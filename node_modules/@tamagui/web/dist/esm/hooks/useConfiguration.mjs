import React from "react";
import { getConfig } from "../config.mjs";
import { ComponentContext } from "../contexts/ComponentContext.mjs";
const useConfiguration = () => {
  const {
      groups,
      animationDriver,
      ...restComponentConfig
    } = React.useContext(ComponentContext),
    {
      animations,
      ...restConfig
    } = getConfig();
  return {
    ...restConfig,
    ...restComponentConfig,
    animationDriver: animationDriver ?? getConfig().animations
  };
};
export { useConfiguration };
//# sourceMappingURL=useConfiguration.mjs.map
